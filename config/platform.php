<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Platform Version
    |--------------------------------------------------------------------------
    |
    | Central source of truth for the current ALTA Web Platform version. The
    | local Marketplace uses this to compute addon platform compatibility
    | (requires_platform / platform_version constraints) without hardcoding
    | the version anywhere in the Marketplace code.
    |
    */

    'name' => 'ALTA Web Platform',

    'version' => '1.0.0',

];
